# 1.1.0 发布准备

2026-09-27。本机 Windows 测试；未使用虚拟机，未上传发布。

## 界面与版本

- 删除默认和声分离开关下的说明及单曲和声分离的悬停说明，保留操作名称。
- 安装器、启动器文件版本、交付目录及源码包命名更新为 1.1.0；保留安装标识用于升级。
- 原有 1.0.0 文件及安装包未改动。
- 1.1.0 离线分卷安装包与源码压缩包已生成，安装分卷已生成 SHA256 校验文件；等待审核，未发布。

## 检查结果

- 15 组规则与资源检查中，12 组首次通过。资源回退测试的模拟分离器更新为当前接口后通过，覆盖分离窗口和音高批量降级。
- 两组历史实歌检查未通过：`supported_pitch_checks`、`event_prior_checks` 依赖《我不难过》原始缓存的固定起点 246.24 秒等事件，当前缓存缺少对应事件。保留失败日志，不计入通过项；需恢复固定样本后复核。
- 当前 8 首歌曲的完整音块回归通过。
- 本机 Qt 界面、旧结果刷新、快速切歌、删除与退出检查通过，真实歌曲库未修改，无 QML 警告。
- 批量分析进程生命周期检查通过；设置面板边界检查通过（Qt 离屏模式）。
- RISE 8 秒片段在新建缓存目录完成实际 CUDA 分析，随后重复分析和关闭和声分离均通过，验证已完成阶段可复用。此项不代表整曲准确率或低配硬件验收。
- 1.1.0 交付目录自带 Python 的界面与真实默认分析检查通过，不依赖开发环境；两种主题无 QML 警告。

日志：`work/release-1.1.0-qa/`。

## 修订检查

- GAME 固定种子 7301，新结果缓存版本为 v3-seed7301。两种人声输入各三次结果完全一致，复用同一模型实例也重建随机状态。旧结果保持可读，不冒充固定种子的重新分析。
- 修复和声切换时人声加载和定位时序；六次往返检查实际解码缓冲、输出非静音、音量及进度差，小于 300 ms。保留音源加载间隙，不声称无缝切换。
- 最新 13 组规则／资源测试通过，详见 `final-unit-results.json`；历史固定样本测试的限制仍保留。
- 主音与曲线读数移至播放线顶端，深浅主题截图检查通过。
- 原曲／人声与和声开关改为单击按钮。
- 和声切换保留原曲播放器作为连续时钟，只重载人声音源；人声重新加载可能有短暂间隙。连续 8 次切换、暂停切换与退出通过，无 QML 警告。测试中出现卡顿的多播放器交接方案已撤回。
- 音域自适应使用当前位置之后有效音高的 5%–95% 分位并留余量。8 首歌各 5 个位置，共 40 个检查，最低覆盖率 94.765%；结果见 `work/release-1.1.0-qa/adaptive-coverage.json`，不构成对任意歌曲的覆盖率保证。
- 安装器保持原 AppId，明确复用安装目录和快捷方式选择，并启用占用文件检查；同一包用于安装与覆盖更新。实际升级验收仍单独列为待完成。

## 封装前待完成

- 恢复两组历史准确性回归的固定样本，消除对可变歌曲库的依赖。
- 任务栏右键菜单名称、V 字图标以及固定后重启行为须用正式安装入口验证；当前 Python 进程的应用标识已设置，但不足以证明菜单名称正确。
- 1.1.0 安装包封装后仍需验证实际安装、升级与任务栏启动行为；交付目录检查不能替代安装升级验收。

当前状态为发布准备中，不是已通过全部发布验收。
