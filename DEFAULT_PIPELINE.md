# 固定默认流程（2026-09-24）

后续未发布源码的计算精简、缓存兼容与音块改动见 [PIPELINE_REFINEMENT.md](PIPELINE_REFINEMENT.md)。下文验证记录仅对应原版本，不代表这些新改动已验收。

按用户要求启用：高质量全部人声提取（Kim MelBand RoFormer）→ 和声分离（BS frazer/becruily）→ RMVPE 连续音高 → GAME 与现有曲线证据修正。保留 CREPE 后端计算供曲线可靠性使用；界面不再提供模型或音高块算法选择。

设置仅保留分析相关的“和声分离”开关，默认开启。提示：如果演唱声部缺失，请关闭和声分离。关闭对应 mel_roformer，开启对应 mel_bs；新导入使用当前开关，已有歌曲点击应用按钮切换缓存或重新分析。缓存分别保存两种音频及曲线，取消不替换原结果。已有歌曲不静默批量重算，旧曲线缺少当前算法时可重新分析升级。

这是基于用户听感选择的产品默认，不表示已解决 RISE 的声部误删。没有接入双轨补回、anvuew 或多歌手分轨实验。

验证：真实 8 秒 RISE 音频完成两种流程、缓存往返、播放位置保留、取消和重试、删除任务、设置持久化与 QML 加载检查；使用隔离测试歌曲库，没有向真实歌曲库加入测试音频。
