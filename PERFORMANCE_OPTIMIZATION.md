# 性能优化与本机对照

2026-09-26。仅修改源码；未重新打包或发布安装包。全部测试在本机 Windows / RTX 4080 上完成，没有使用虚拟机。

## 真实 1.0.0 整曲累计对照

保留的 1.0.0 交付源码与当前开发版，在同一运行环境下对 RISE、花海、小镇姑娘三首整曲进行两轮反向顺序测试，结果缓存独立且为空。包含进程启动到退出的总耗时平均 **412.574→235.258 秒，减少 42.98%**；首份分析结果等待时间平均 **114.388→68.333 秒，减少 40.26%**。峰值 RSS 增加约 1.18 GiB，PyTorch 峰值已分配显存增加约 124 MiB。

本次是不同真实版本的完整流程对比，包含早期多余计算的移除及音块规则变化，不是纯粹关闭几个性能开关，也不代表完全无损。详细方法、输出差异、两轮记录和适用范围见 [整曲版本对照](PERFORMANCE_RELEASE_COMPARISON.md)。累计速度以该统一口径实测为准，不累加下面各轮百分比。

## 计时与候选加速对照（第七轮）

正式流水线新增可选 `stage_cache.timing` 记录，使用单调墙钟记录阶段起止时间。`busy_union_seconds` 对重叠区间只计一次，`other_seconds` 保留导入、清理、调度等未单独划分的耗时；GAME 与 GPU 音高分析的耗时不能直接相加当作总时间。分离资源记录新增模型加载、分离与导出、读取校验及 CPU 缓存转存时间。缓存命中时的 `recorded_execution` 是原执行的历史信息，`from_cache` 明确标记，不能算成本次推理。旧结果和旧缓存缺少这些字段时仍可使用。

基线已经包含混合精度、分离模型复用和队列进程复用。三首 30 秒片段按顺序运行，使用独立空缓存、固定随机种子，每种线程数各测三轮。剖析器测量不混入性能对照。

| GAME 线程数 | 三首队列耗时中位数 | 相对默认耗时变化 |
|---|---:|---:|
| 4（默认） | 31.487 秒 | — |
| 2 | 35.730 秒 | 增加 13.5% |
| 8 | 30.392 秒 | 减少 3.5% |

8 线程未达到预设 5% 的全队列收益门槛，保留默认 4 线程、不自旋。4 线程第一首中位数为 13.096 秒，末首重复命中缓存约 0.325 秒；这些数值是分析调用耗时，不包含 Python 进程启动。采样峰值 RSS 约 4193 MiB，PyTorch 峰值已分配显存约 2324 MiB；不是最低配置声明，也不包含所有 GPU 驱动分配。新基准脚本另外记录导入准备和包含缓存复查的完整进程时间。

GAME 四个 ONNX 会话复用试验未通过质量门槛：每首都重新设置相同随机种子，复用后的第二、三段原始音符数量仍从 28、102 变为 29、97。虽然省掉后续约 0.75 秒的会话创建，但未接入默认流程。不能把会话加载收益直接当作全流程收益。

独立 CUDA 剖析覆盖两种分离模型各一个窗口，实际调用高效注意力内核。MelBand 的逐元素乘法、矩阵乘加、复制及拼接占较多设备时间；BS 的乘法、复制、拼接及注意力同样明显。Python 中 `istft` 的累计时间可能含同步等待，不能据此判断纯 GPU 瓶颈。单窗口剖析不能替代完整歌曲计时，累计分配量也不能当作峰值显存。

实验和原始记录：`tests/performance_round7.py`、`tests/game_session_trial.py`、`tests/batch_separation_trial.py`、`tests/round7_compare.py`、`tests/round7_report.py`；输出位于 `work/performance-round7/`。实验批处理适配器不在安装源码清单中。

### 双片段分离：未启用

试验真正将两个同长片段堆叠进入 RoFormer，保留原切片位置、顺序、窗函数及重叠。仅在 CUDA 可用显存不少于 8 GiB 时尝试；尾段单独处理，批量 OOM 在提交输出前退回单片段，避免重复累加。没有降低精度、重叠或修改模型权重。

30 秒片段初筛的曲线和音块 10 ms 网格通过对照，但三首完整歌曲未通过质量门槛，因此按停止条件结束试验，没有继续重复三轮，也不将这一次耗时作为稳定提速证据。

| 三首整曲合计（RISE、花海、小镇姑娘） | 原单片段 | 双片段试验 |
|---|---:|---:|
| 完整分析调用耗时 | 236.811 秒 | 233.405 秒 |
| MelBand 阶段 | 68.227 秒 | 67.527 秒 |
| BS 阶段 | 85.993 秒 | 86.106 秒 |
| 采样峰值 RSS | 4542 MiB | 4552 MiB |
| PyTorch 峰值已分配显存 | 2324 MiB | 3269 MiB |

两级分离合计仅减少约 0.4%，总耗时减少约 1.4%，显存增加约 944 MiB。MelBand、BS 均未显示足够收益。模型阶段采用同一进程内顺序队列、独立空结果缓存，复查时所有阶段命中缓存。阶段时间含缓存校验和结果保存，GPU 设备时间另由剖析器记录。

质量差异：RISE 最终音块 310→308，10 ms 网格 138 帧不同；花海音块数量相同，但边界覆盖有 4 帧不同；小镇姑娘音块 776→774、48 帧网格不同，并有发声掩码变化、两帧曲线差异超过 50 音分（86.58 秒、176.89 秒，最大约 6.84 个半音）。这些是相对单片段基线的差异，不代表已人工判定哪种结果正确，但已不满足本轮保持结果的标准。

因此正式流程仍用原单片段分离、GAME 每首新建会话及 4 线程。本轮保留精确计时与可复现试验工具，没有宣称新增默认加速。分离资源细分计时描述成功的那次尝试，`successful_attempt` 标识尝试序号；失败重试和清理开销包含在外层阶段墙钟时间中。

回归通过：计时并集与异常记录 2 项、批量/短尾段/静音/低显存/OOM 不重复提交 3 项、规则与缓存 10 项、精度回退 7 项、模型池 5 项，以及输入兼容、并行失败清理、真实 Qt 队列复用/取消/失败重启/删除/空闲退出。原生 Windows 界面检查无 QML 警告，旧结果发现、切歌、播放、快速删除及退出正常，实际歌曲库校验未变化。未使用虚拟机，未重新打包。

## 连续队列分离模型复用（第六轮）

`analysis_worker.py` 在其生命周期内启用 `separation_pool.py`。模型推理后移到 CPU，下首需要相同模型时移回 CUDA，避免重建网络和重读权重。不是把两级模型同时常驻显存。GUI 不持有模型；单次 CLI 分析默认不启用缓存。

- 最多两个模型，参数及 buffer 张量合计上限 1.25 GiB。缓存前要求可用物理内存不少于 8 GiB 加待缓存模型张量大小；取用时低于 8 GiB 就清空缓存。
- 缓存键包含设备、窗口、精度及权重/YAML 路径、大小、修改时间。不同配置不会混用。
- 清除每首歌的输入和输出数组、更新下一首输出目录。推理失败清空模型池；可选 CPU 转存失败不会破坏已经完成的结果。正常 EOF/异常退出在 finally 清空，取消时整个工作进程退出。
- 初次尝试 1 GiB 上限不能容纳两模型，发生轮流挤出，没有收益，已调整；该失败试验不计入最终结果。

三段不同的 30 秒音频（RISE、花海、小镇姑娘），两级分离，结果缓存均未复用。基线与新方案都已使用上一轮 AMP：

| 测量 | 每首重新加载模型 | 有界 CPU 模型复用 |
|---|---:|---:|
| 第一轮 | 20.720 秒 | 18.503 秒 |
| 第二轮 | 20.632 秒 | 18.525 秒 |
| 平均 | 20.676 秒 | 18.514 秒 |

连续三段分离耗时减少约 **10.5%**。第二、三段两级模型准备合计由约 1.5 秒降至约 0.17 秒，准备时间包括复用时从 CPU 搬回 GPU。首段仍需加载并额外转存，单首分析不会获得同样收益。测试包含缓存转存和阶段清理；不把加载环节提速当作全流程提速。

缓存张量约 1.04 GiB。进程峰值 RSS 从约 2199–2200 MiB 增至 2411–2417 MiB；这不表示缓存仅占 214 MiB，基线峰值本身已包含模型加载时的临时占用。显存峰值约 2246 MiB，两个模型缓存后空闲已分配显存约 8.125 MiB，与基线相同。转存未增加持续 GPU 模型占用；Windows/PyTorch 分配器保留内存不等于活动模型张量。

最终两次复用测试的六组分离音频与前一轮 AMP 输出逐采样完全相同。五项模型池测试覆盖取用/释放、内存压力、数量和容量上限、配置隔离及可选转存失败；原精度回退和输入重试检查通过。

真实三任务分析进程的完整流水线通过：各阶段结果缓存均未命中，第二首起两级模型均命中模型池，分离音频和最终音块与前轮 AMP 对照完全一致，工作进程正常退出。本轮该完整队列用时 33.46 秒，因没有同步测量完整队列的禁用模型池组，不据此声明全流程提速比例。原生 Qt 的连续队列、失败换进程、取消、删除和空闲退出检查，以及 10 项基础回归通过。

数据：`work/performance-round6/baseline1`、`baseline2`、`reuse3`、`reuse4`；`reuse1` / `reuse2` 为 1 GiB 上限的失败候选。脚本：`tests/performance_round6.py`、`tests/separation_pool_checks.py`、`tests/separation_pool_pipeline.py`。

## 自适应混合精度（第五轮）

默认分离新增 `separation_execution.py`：CUDA 计算能力至少 7.0 且非 GTX 系列时，先做小规模 FP16 矩阵和 SDPA 有限值自检，通过后采用 AMP；其余设备及自检失败保持 FP32。此为功能能力筛选，不保证每种合格显卡都更快。硬件/驱动整体兼容仍由原有硬件自检先判断。没有绑定 RTX 4080 型号，没有新增用户设置，AMD/Intel GPU 未由此获得加速支持。

AMP 阶段允许融合注意力和普通数学实现，由 PyTorch 按实际输入条件选择。当前三段实测中 AMP 与 AMP 加自动注意力配置的输出相同，额外速度差异很小，因此不宣称 Flash Attention 带来了这轮提速，也没有证明所有实际调用都使用 Flash 内核。未强制改变注意力张量精度。

不支持的低精度内核或非有限结果会在相同窗口以 FP32 重试；显存不足沿用减小窗口，在 CPU 回退前保留 FP32 尝试。磁盘错误等无关错误继续向上报告。每个分离阶段记录实际设备、精度和精度回退情况；缓存包含策略版本、请求精度和计算能力，避免 FP32 和 AMP 结果误用。已有歌曲不自动重算。模型权重、重叠和音块规则未变。

RISE、花海、小镇姑娘各 30 秒，独立空输出目录，三段连续分离（MelBand + BS）合计：

| 轮次 | FP32 | AMP + 自动注意力配置 |
|---|---:|---:|
| 第一轮 | 30.524 秒 | 20.101 秒 |
| 第二轮（反向顺序） | 30.381 秒 | 19.897 秒 |
| 平均 | 30.453 秒 | 19.999 秒 |

合计耗时减少约 **34.3%**，只代表本机这些片段的分离阶段，不是完整分析提速。单独 AMP 第一轮为 20.074 秒。峰值已分配显存从约 2200 MiB 增至约 2242 MiB；AMP 并未在本实现中降低峰值显存。

质量对照（FP32 作为参考，不是人工真值）：

- 人声波形相对差异 SNR：MelBand 约 67.9–74.1 dB，经过 BS 后约 54.9–71.2 dB；非逐采样一致。
- 三段有声掩码一致；连续曲线在双方有声帧的绝对差异第 95 百分位分别约 0.070、0.029、0.060 音分。
- 小镇姑娘片段相对 3.86–3.90 秒（原曲约 2:38.86–2:38.90）共 5 帧变化超过 50 音分，最大约 692 音分，占双方有声帧约 0.21%。没有真值，不能认定任一版本更准，也不能称整体无损。
- 固定 NumPy、PyTorch、ORT 随机种子后，最终音块数量分别 34、31、102；10 ms 时间格上的音块覆盖及音高均一致。不代表事件边界在任意小时间尺度都完全一致。
- 新正式路径的 30 秒完整分析通过：分离结果与候选一致，音块有效，重复分析命中缓存，切换精度后两级分离缓存隔离。
- 7 项策略/回退测试、输入校验和重试检查、10 项基础回归、并行分支失败清理检查通过。
- 原生 Qt 控制器的连续任务复用、失败后重启、取消、排队删除和队列清空退出检查通过。

其他显卡仅做了策略分支模拟，包括 GTX 1060、GTX 1650、RTX 2060、RTX 3050 Laptop、RTX 4060 等；**不是这些硬件的实机验收**。当前样本不足以证明所有歌曲质量均不退步，发布前仍需扩大曲目与硬件验证。

记录：`work/performance-round5/`。速度脚本 `tests/performance_round5.py`，波形/下游对照 `tests/separation_quality5.py`、`tests/separation_downstream5.py`，正式流水线 `tests/adaptive_pipeline_checks.py`，策略测试 `tests/separation_execution_checks.py`。

## 保留的改动

- 切歌只解析当前显示的曲线；旧实验曲线按需读取。原始文件与隐藏的实验接口仍保留，曲线值没有重新采样。
- GAME 四个 CPU 推理会话关闭空闲线程自旋，保持原有线程数和模型参数。
- 两级分离结束后，在资源充足时，让 CPU 上的 GAME 与 GPU 上的 CREPE / RMVPE 重叠执行。最终后处理仍等待两边完成。
- 并行仅在 CUDA 可用、至少 8 个逻辑 CPU、可用物理内存大于 8 GiB 加四份立体声音频估算空间时启用；CPU 模式、内存不足或无法查询内存时保持顺序执行。
- GPU 分支失败时通知 GAME 停止，并等待后台线程退出。界面取消整个分析进程仍会停止该进程内全部线程；未完成阶段不发布缓存清单。

试验过分离阶段使用 `torch.inference_mode`，没有测到收益，已撤回。没有降低分离重叠、精度或模型规模，也没有改变音块规则。

## 首次分析对照

同一段 30 秒 RISE 原曲音频，各轮使用独立的新进程与空阶段缓存，固定 NumPy、PyTorch 和 ONNX Runtime 随机种子 7301。对照组恢复原来的 GAME 自旋和顺序调度。

| 场景 | 两次测量 | 平均 |
|---|---|---:|
| 原顺序流程 | 19.606、19.751 秒 | 19.678 秒 |
| 新流程 | 17.558、17.485 秒 | 17.521 秒 |

耗时减少约 **11%**，每次少约 **2.16 秒**。这是没有命中模型结果缓存的测量；不能与重复分析的缓存加速混为一谈，也不能直接推广为所有歌曲或配置的提速比例。

另一次进程树 RSS 采样约为 2812 MiB（顺序）和 2809 MiB（新流程），采样间隔 50 ms。本片段没有明显增加进程峰值；不代表长歌曲或低内存机器一定相同。这轮内存采样的顺序组期间存在另一回归进程，因此其耗时不纳入上面的速度对照。

## 切歌对照

7 次实际切换已有歌曲，均使用已生成的展示缓存：

- 原流程平均 217 ms，范围约 156–278 ms。
- 按需读取后平均 53 ms，范围约 39–63 ms。
- 本次耗时减少约 **75%**。测量包含切歌调用内的曲线读取和播放源切换，不代表音频设备出声延迟。

## 输出与回归检查

- 固定种子的四轮对照中，分离后音频采样值完全相同；GAME 原始音符和最终音块 JSON 完全相同。
- 曲线时轴、有声掩码、音高与分数通过 `atol=1e-4, rtol=1e-5` 对照。
- CREPE 和 GAME 都包含随机行为。未控制随机种子的独立重跑不能直接作为调度改动影响准确性的证据；正式程序没有为本次优化改变其随机策略。
- 10 项规则、缓存及资源保护测试通过。
- 原生 Windows 界面的切歌、旧格式库读取、后台补齐、删除和退出检查通过；原歌曲库未改动。
- GAME 失败备用流程、仅调整后处理不重跑模型的检查通过。
- 注入 GPU 分支失败后，GAME 收到取消信号并退出，没有留下运行中的 CPU 工作线程。

数据位于 `work/performance-round2/summary.json`，对照程序为 `tests/performance_round2.py`，失败清理检查为 `tests/performance_scheduling_checks.py`。基准目录不得复用已有阶段缓存；程序会拒绝已经存在的测量标签。

## 连续导入优化（第三轮）

连续队列复用一个 Python 分析进程，减少运行库导入、硬件探测和 CREPE 重复加载。歌曲仍逐首处理，两级分离模型和 RMVPE 按原阶段释放；没有同时分析多首歌曲。队列清空后关闭输入并退出进程，失败或取消后使用新进程继续后续任务。每首歌独立保存进度、日志和结果，显存峰值统计在每首歌开始时重置。

本机 RTX 4080，三段不同的 30 秒原曲：RISE 90–120 秒、花海 160–190 秒、小镇姑娘 155–185 秒。每组使用独立空缓存，全部模型阶段确认未命中缓存；固定 NumPy、PyTorch、ONNX Runtime 随机种子。基准包含进程启动到退出，第二轮颠倒组别顺序：

| 测量 | 每首启动进程 | 队列复用进程 |
|---|---:|---:|
| 第一轮 | 54.458 秒 | 45.519 秒 |
| 第二轮（先测复用） | 55.069 秒 | 46.022 秒 |
| 平均 | 54.763 秒 | 45.771 秒 |

三段合计节省约 **8.99 秒，耗时减少 16.4%**。这是相对第二轮优化后的连续队列收益，不是单首歌曲推理提速，也不代表完整歌曲或其他硬件上的固定比例。

进程树峰值 RSS：每首启动约 2826 MiB，复用约 2833–2836 MiB。PyTorch 峰值已分配显存从约 2200 MiB 增至 2285 MiB，峰值预留显存从 3652 MiB 增至 3736 MiB，主要来自 CREPE 的现有模型缓存；它会随队列进程退出而释放。保留原有显存不足降批量和 CPU 回退机制。未做低配置长队列压力测试，不据此公布最低配置。

两轮对照均确认：分离音频采样值完全相同，GAME 原始音符和最终音块 JSON 完全相同，曲线通过 `atol=1e-4, rtol=1e-5` 对照。正式程序未改变模型、精度、阈值或随机策略。

测试入口：`tests/performance_round3.py`（`--reverse` 颠倒顺序）、`tests/performance_round3_equivalence.py`。结果保存在 `work/performance-round3/`，测量标识为 `1790427592146613400` 和 `1790427836046937900`。`tests/batch_worker_checks.py` 通过真实 Qt 控制器验证同进程连续完成、取消/失败后换新进程、删除排队任务和空闲退出；基础规则及并行失败清理检查通过。测试使用隔离数据目录，不修改用户歌曲库。

原生 Windows 界面回归也通过：切歌、播放、旧库后台补齐、快速切换与删除、退出清理正常，无 QML 警告；原歌曲库校验值未改变。

## 后续方向

本机 30 秒剖析中，两级分离约占 63% 的首次分析耗时，仍是主要瓶颈。下一步应剖析完整歌曲中分离阶段的音频切片、数据搬运和重叠拼接，优先减少重复操作。当前没有引入长期常驻的大型分离模型、半精度或减少重叠。

## 分离输入处理（第四轮）

`vocal_separator.py` 直接把已解码并缩放的 float32 立体声交给原库的数组输入校验，省去每一级临时 `input.wav` 的写入和重读。输入保持原 FLOAT WAV 的 float32 舍入，输出仍走原 soundfile 写入器，保留音频校验、归一化、原子保存和增益还原。文件名仅用于生成输出名称；已固定的 WAV 写入器不再通过临时输入查询时长。

没有修改第三方运行库文件。模型、精度、切片、重叠、重试与缓存版本保持不变；最终分离结果文件仍保存，以便阶段复用。4 分钟、44.1 kHz、双声道音频，两级合计减少约 161.5 MiB 临时输入写入及同量读取，实际磁盘收益取决于文件系统缓存。

曾试验在显卡上保留整段输入与拼接缓冲区：60 秒 RISE 两级分离从 24.425 秒变为 24.314 秒，显存峰值却增加约 105 MiB，收益不足，已撤回。该测试没有作为保留改动的提速证据。Python 调用剖析中的 CUDA 算子耗时可能包含等待，不能据此把 `istft` 的累计时间当作其纯 GPU 计算时间。

本轮测试及剖析保存在 `work/performance-round4/`，完整模型测量入口为 `tests/performance_round4.py`，采样值对照为 `tests/performance_round4_equivalence.py`，输入兼容检查为 `tests/separation_input_checks.py`，完整流水线对照为 `tests/performance_round4_pipeline.py`。所有测试在本机执行，无虚拟机；没有重新构建安装包。

60 秒 RISE 原曲（90–150 秒）、独立进程、两级分离全部重算的结果：

| 测量 | 原输入路径 | 内存输入 |
|---|---:|---:|
| 第一轮 | 24.425 秒 | 23.540 秒 |
| 第二轮 | 24.352 秒 | 23.491 秒 |
| 平均 | 24.388 秒 | 23.515 秒 |

节省约 0.87 秒，分离阶段耗时减少约 **3.6%**。测量使用 Python 调用剖析器，范围从调用第一级分离到第二级返回，包含模型加载及清理，不包含 Python 启动和后续音高分析。结果来自 `baseline1` / `baseline3` 与最终实现的 `memory3` / `memory4`；中间候选、失败试验与其他测量未混入平均值。该收益偏向冷进程的输入准备，不能直接推广到长歌曲、连续导入或全流程耗时。

峰值 PyTorch 已分配显存均约 2200 MiB，预留显存均为 3652 MiB。两轮分离输出逐采样完全相同；30 秒独立空缓存完整流水线的音频、GAME 音符及最终音块完全相同，曲线通过 `atol=1e-4, rtol=1e-5` 对照。float32 输入、立体声布局、短输入、空/非有限输入校验、模拟显存不足重试与增益还原检查通过；10 项规则及缓存回归通过。原歌曲库未改动。
