# 本机回归测试记录（2026-09-26）

本轮直接使用本机 Windows、现有 Python 环境及 NVIDIA GPU，未使用虚拟机。测试歌曲库、输出与缓存位于 `work/local-refinement-qa`；原歌曲库未改动。没有重新封装或发布安装包。

## 结果

- 9 项自动化规则及缓存测试通过：稳定漏块、局部长音变音、短促转折、短 GAME 音符保护、气声/静音证据不足、尖峰/普通颤音、八度保护、缓存损坏与版本失效、并发及强制中断后恢复。
- 8 首现有歌曲完成整首后处理及缓存复用；输出时间有序、不重叠、不越界，原曲线文件摘要保持一致。
- 原生 Windows Qt 界面通过切歌、静音播放/暂停、后台补齐、快速切歌与删除、原始旧格式库读取、后台任务运行中退出检查；没有 QML 报警。检查了窗口截图。
- 正式命令行入口成功生成格式 v2 的歌曲结果和完整索引，全部模型阶段命中缓存。
- 真实 8 秒 RISE 原曲片段完成两级分离、CREPE full、RMVPE 连续追踪和 GAME；未触发 GAME 备用流程。
- 关闭和声分离复用了已提取的全部人声。仅更新后处理版本不调用 GAME，并复用了响度缓存。
- 人为注入 GAME 失败后，成功生成曲线备用音块并记录失败状态。

## 本机耗时

| 场景 | 耗时 |
|---|---:|
| 8 秒 RISE，首次完整分析 | 17.596 秒 |
| 同片段、同参数复用缓存 | 0.293 秒 |
| 同片段关闭和声分离，复用全部人声并重算下游 | 2.384 秒 |
| 8 首已有歌曲首次整理音块和展示缓存 | 每首 0.513–1.288 秒 |
| 同批歌曲复用后处理缓存 | 每首 0.116–0.181 秒 |
| 原生界面已有展示缓存切歌 | 约 0.156–0.278 秒 |

完整分析使用 CUDA。该模型测试进程记录的 PyTorch 显存 allocated 峰值约 1764 MiB，reserved 峰值 2292 MiB；这是分配器统计，不等于整机/进程总内存。这些数值来自本机与短片段，不能推导低配最低要求、整首冷分析耗时或普遍提速比例。切歌统计不包含未切换歌曲的空操作。

## 精简前后对照

使用原实验入口再次运行 tiny + full 和相同分离参数：人声输出最大绝对误差为 0。CREPE full 的置信度和有声掩码一致；RMVPE 连续曲线在指定数值容差内一致。

最初逐点比较 CREPE 音高失败。排查发现 torchcrepe 解码会叠加每次独立的 ±20 音分随机微扰，本次两次输出最大差异为 32.12 音分。固定相同随机状态后，先运行 tiny 再运行 full 与直接运行 full 的结果一致。这是对照方法的修正，没有关闭或更改正式模型的解码行为。

## 尚未覆盖

- 真实低配硬件、CPU 完整耗时、真实显存不足与长时间批量导入。
- 新安装包安装/升级；本轮未打包。
- 人工逐音标注或盲听评价。结构验证和输出数量不能证明音高准确率已经提高。

复现入口：`tests/pipeline_refinement_checks.py`、`tests/pipeline_refinement_integration.py`、`tests/pipeline_refinement_equivalence.py`。详细数据与日志位于 `work/local-refinement-qa`。
