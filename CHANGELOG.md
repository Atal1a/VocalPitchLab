# 更新记录

## 1.1.0（待发布）

- 默认和声分离并入设置的功能选项，播放区增加当前歌曲的独立和声分离选择；复用已有版本，缺少结果时加入分析队列，不改变新歌曲默认设置。统一下拉框边框、悬停反馈与设置行样式。
- 顶部改为时间、音域跨度、音域中心三个控制区，支持滚轮、横向拖动和 Shift 精调；整首视图可恢复之前的时间跨度，音域显示当前上下界。
- 校正长音提前标成高音的起始边界：在连续上行且音高证据充分时拆分低音与目标高音，滑音保留曲线。8 首全曲对照仅《小镇姑娘》03:43 和《花海》02:29 各一处变化；展示缓存升级为 v7，详见 ONSET_BOUNDARY_FIX.md。
- 小幅收紧自动短转折补块，最短有效音高支持从 60 ms 调整为 70 ms；GAME 原始短音保护和持续颤音处理保持原规则。
- 修复连续宽颤音被补成峰谷短音块的问题：识别有充分人声证据的规律往返波动，在漏块区域按中心音高补充持续音块；保留原始曲线、GAME 音符边界和孤立转折。音块缓存升级为 v6，旧歌曲自动更新展示结果。
- 增加分析阶段墙钟时间、并行时间并集和分离模型加载/导出细分记录；旧结果无需迁移，缓存命中的执行详情明确标为历史记录。
- 连续导入在内存充足时复用 MelBand / BS 模型，模型闲置时移到 CPU；最多两个、张量上限 1.25 GiB，队列结束释放。减少重复加载，保留内存不足及失败时的清理。
- 人声分离增加按 CUDA 能力选择的混合精度与注意力后端策略；旧显卡保留 FP32，不支持的内核或非有限输出回退 FP32，显存不足继续减小窗口。
- 分离缓存区分执行策略；本机三段音频两轮对照分离耗时减少约 34%，质量与硬件验证范围见 PERFORMANCE_OPTIMIZATION.md。
- 两级人声分离直接使用已解码的 float32 音频，省去临时输入 WAV 的写入和重复读取；保持原模型、重叠、精度与输出校验。
- 连续导入复用后台分析进程，队列结束释放资源；本机三段音频对照平均耗时减少约 16%，保留任务取消、失败重启和队列删除。
- 按需读取当前音高曲线，减少切歌时的文件解析。
- 资源充足时重叠执行 CPU 音符识别与 GPU 音高识别，关闭 GAME 空闲线程自旋；本机 30 秒片段首次分析耗时减少约 11%，详见 PERFORMANCE_OPTIMIZATION.md。

- 精简默认分析，移除不参与正式结果的模型计算和实验输出。
- 增加分阶段缓存，复用已完成的人声分离、音高及音符结果。
- 缓存最终音块、曲线证据和响度，旧歌曲在后台补齐。
- 调整长音局部变音、漏块和有充分证据的短促转折处理。
- 已通过本机规则、缓存、旧库兼容及短音频完整流程检查；详细记录见 LOCAL_REFINEMENT_QA.md。

## 1.0.0

- Windows 本地人声音高分析，音高曲线与主要音符块结合展示。
- 固定默认分析流程，保留和声分离开关。
- 原曲与分离人声播放、循环、音量控制及标准音试听。
- 歌曲库管理、任务取消、查看位置记忆、深浅主题。
- 独立 VocalPitchLab.exe 入口，统一 V 字应用图标与安装命名。
- 完整离线分卷安装，内置模型、Python、CUDA 运行库与 VC++ 安装组件。

本轮交付封装按用户要求未追加运行测试。CUDA 兼容性依设备与驱动而定；未公布实测最低配置。完整二进制分发的第三方许可事项见 THIRD_PARTY_NOTICES.md。
