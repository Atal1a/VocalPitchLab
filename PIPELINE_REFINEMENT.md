# 默认流程精简与音块修正

状态：代码已完成本机回归检查，详见 [LOCAL_REFINEMENT_QA.md](LOCAL_REFINEMENT_QA.md)。已发布的 1.0.0 安装包未改变。

## 第一组：计算和缓存

- 正式入口改用独立生产流程，保留完整 CREPE 证据、RMVPE 连续曲线和 GAME。实验对比仍由原有实验脚本提供。
- 正式分析不再运行 CREPE tiny、融合候选、实验窗口生成、音符 CSV 导出或 Matplotlib 对比绘图。
- 阶段缓存位于用户数据目录的 `results/stages`，分别记录解码、全部人声、BS 主人声、CREPE、RMVPE、GAME、最终音块/证据及响度/播放增益。
- 阶段以内容摘要、算法版本和参数寻址；下游使用上游实际输出摘要，避免重建上游后错误复用下游。
- 进程锁防止相同阶段并发写入；完成清单最后原子发布，读取时核对文件长度与摘要。进程退出会释放锁。失败或取消产生的未完成工作目录不作为缓存，也不自动删除。
- GAME 失败时记录错误并使用连续曲线生成备用音块；下次正式分析仍会重试 GAME，不把失败记成成功缓存。
- 正式结果新增 `result_format: 2`、`stage_cache`、`game_notes`、`game_notes_version` 和 `presentation`。`main_notes` 仍保留，指向最终音块，旧歌曲格式继续读取。
- 曲线文件和 GAME 原始结果保持独立，不被修正结果覆盖。调整音块规则只失效展示/后处理缓存，不失效模型输出。
- 切歌直接读取已整理的结果。旧歌曲缺少有效展示缓存时，由单独的后台进程补齐；当前歌曲、分离结果身份不匹配时不安装迟到结果。关闭程序会停止后台进程。
- 后台补齐仅处理已有音频、曲线和已有 GAME 结果，不运行神经网络。没有旧 GAME 结果时使用曲线备用音块。
- 播放响度和人声音量补偿一起缓存，切歌不再同步扫描整首音频。

## 第二组：音块与曲线的一致性

- 后处理版本更新为 `game-local-evidence-v5`，参数集中在 `RECONCILE_SETTINGS`。
- 音块证据使用独立的时间连续人声支持掩码，不再把经过平滑的显示深浅当作置信度；该证据是启发式支持，不是校准后的概率。
- 稳定候选至少 0.30 秒，有效帧覆盖至少 90%，目标音附近支持至少 80%。可靠长音冲突或漏块至少持续 0.35 秒。
- 长音局部修正不再依赖整条事件 75% 的覆盖门槛。保留未替换部分，目标音块不重叠、不超出歌曲时长。
- 新增短上探/回落候选，要求至少 1.5 个半音的转折和目标音附近至少 60 毫秒的支持。仅在连续有效人声中允许不超过 30 毫秒的短桥接；候选不长于 0.5 秒。
- 原有短 GAME 事件优先，避免重复补块。普通颤音、单帧尖峰、静音间隔不因该规则生成额外音块。
- 接近八度及更大的冲突不使用新增覆盖规则；此前的多声部选择问题仍未处理。
- 修正来源保留为 `curve_split`、`curve_fill` 或 `curve_turn`。原始曲线不改值、不额外平滑，不改变曲线显示的响度映射。

## 兼容和版本管理

旧库不批量迁移、不删除原音频或旧结果。打开歌曲后才按需补齐后处理缓存。旧版分析的分离结果没有伪装成新阶段缓存；首次通过新流程重新分析旧歌曲时，可能仍需运行模型。

更新模型权重、推理参数或实现时，须更新对应阶段版本；更新连续解码器须更新 RMVPE 阶段版本；更改 GAME 转录须更新 GAME `VERSION`，仅修改后处理则更新 `REFINEMENT_VERSION`。分离的模型精度、重叠、批量和显存回退策略保持原样。

新增模块已加入统一安装源码清单。本轮没有生成新的安装包、源码压缩包或 GitHub 发布。

## 验收场景

下列为验收清单；已执行范围、结果和剩余限制以 LOCAL_REFINEMENT_QA.md 为准。结构与缓存测试不代替准确率评价。

1. 同一歌曲新旧流程保留阶段的音频、音高和 GAME 原始结果对比，确认精简没有改变模型参数。
2. 相同文件重复分析复用缓存；关闭和声分离复用全部人声；仅改后处理版本时不运行分离或音高模型。
3. 同一阶段并发请求、处理中取消、清单损坏及文件被截断时，不接收半成品、不污染已有结果。
4. 旧库切歌、连续快速切歌、分析版本切换、删除歌曲以及退出程序时，后台结果不得装到错误歌曲上。
5. 《花海》长音变音与稳定曲线漏块；《我不难过》短平台与上探；《小镇姑娘》轻声、低音和换气；RISE 的八度争议保持原策略。
6. 合成颤音、孤立尖峰、静音、时间断点、缺少可靠性证据及保护短音的情况；输出不重叠、无负时长，输入数据不被修改。
7. 分别记录冷分析、缓存命中、后处理刷新和切歌耗时，并记录峰值内存与显存；不把缓存命中速度当作首次分析速度。
