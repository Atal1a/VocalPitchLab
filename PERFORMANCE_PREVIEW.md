# 性能预览：不是最低配置声明

2026-09-24，在同一台开发机（RTX 4080 16GB、24 逻辑 CPU、63.7GiB RAM）测试。输入为 RISE 已提取人声的 20 秒片段，仍执行默认完整分析链路，包括再提取全部人声、BS、CREPE、RMVPE、GAME，因此不是原始伴奏混音的质量评估，也不是整曲耗时测试。

| 模式 | 完整进程耗时 | 进程树 RSS 采样峰值 |
|---|---:|---:|
| CPU，OMP/MKL 环境变量设 4 线程 | 174.07 秒 | 2544.52 MiB |
| CUDA，PyTorch 分配预算 4GiB | 15.14 秒 | 2677.20 MiB |

CUDA 测得 PyTorch 峰值已分配 2016.74 MiB、峰值保留 3652 MiB；预算不是物理 4GB 显卡，也不包含所有非 PyTorch GPU 分配。CPU 并未变为低端 CPU，也没有限制宿主物理 RAM。RSS 每 200ms 采样可能错过短暂尖峰；测试期间存在安装器编译负载，结果仅供内部预览。

两项均正常完成。4 秒启动测试也通过。不能据此线性推算全曲速度，不能公布“4GB 显卡/8GB 内存最低配置”。缩短分离窗口会改变上下文，可能带来结果差异；需继续比较低资源路径质量。

分离、CREPE、RMVPE 的模拟 OOM 重试测试通过；非显存错误会继续报错，不被吞掉。尚未完成真实低配置机器、整曲峰值、长期稳定性及 CPU 失败恢复验证。

原始记录位于 outputs/resource-benchmark（不进入公开仓库）。下一步应由可复现安装包在实体低配电脑跑固定整曲，再填写推荐配置。
