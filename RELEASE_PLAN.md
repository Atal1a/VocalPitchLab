# 正式版发布准备

更新：2026-09-25。当前优先完善发布条件，暂停重复编译安装包。

最新决定：补齐 VC++ 离线依赖后不再进行测试，优先交付准备。本轮已保存官方 14.51.36247 x64 文件，接入安装器版本检测、离线补装、管理员权限、失败提示、重启处理和日志，并登记来源/哈希/第三方说明。没有运行补齐后的测试，没有重新编译，dist 中现有文件仍是旧版。下一次统一构建需纳入全部当前源码、GPL 许可证和该依赖；不要直接发布旧包。未验证项保留事实状态，不因停止测试而改为通过。

干净环境更新：固定流程已在独立 Python 环境从 81 个哈希锁定 wheel 安装并通过分析、播放、钢琴 MIDI、取消与持久化检查；Python 分析进程禁网测试通过。详见 CLEAN_BUILD.md。该结果不等于全新 Windows 验证，也未将旧预览构建器切换为正式构建。

许可核查更新：见 MODEL_LICENSE_REVIEW.md。GAME 权重已确认 CC BY-NC-SA 4.0（并非 MIT）；BS 缺少明确分发授权；RMVPE 发布仓库除 MIT 标签外还有研究用途措辞，需要澄清范围。首次核查已完成，不代表这些许可缺口已解决。

## 已固定的产品与分发方案

- 自有代码按用户选择采用 GPL-3.0-only；第三方条款继续保留。已有预览安装包包含旧许可快照，不作为正式发布包，暂停重新编译。

- Windows 本地 Qt Quick/QML + Python，默认分析流程保持不变。
- 完整离线安装，内置正式模型和 CUDA 版 PyTorch；暂不做按需下载。
- GitHub Releases 发布 Setup.exe + 三个数据分卷，单卷小于 2 GiB。
- 普通用户只需显卡驱动，不要求自行配置 CUDA Toolkit 或 Python。
- 暂无公开正式版；现有安装器均为本地预览，不可直接上传。

## 已有证据

- 保守裁剪删除 909,539,011 字节开发/实验文件，正式模型和 Torch DLL 校验通过。
- 分卷安装成功，包内运行时完成 20 秒 CUDA 分析及原曲/人声播放；卸载保留数据。见 outputs/github-installed-validation。
- 裁剪运行时 pip check 通过。resources/runtime-windows-cu124.lock.txt 是该环境的版本快照，尚不是带 wheel 哈希的完整可复现安装锁。
- 新增实际 GPU 矩阵/卷积运算自检；失败回退 CPU，任务显示原因，分析目录保存 hardware.json。
- 显存不足转用 CPU 会明确提示；驱动失败、无 GPU、强制 CPU 与 OOM 重试检查通过。
- 实际 BS 导入、高质量重分析、缓存切换、取消、重试、删除、持久化和 QML 回归通过。
- 新增 installer/release_check.py 输出发布条件与缺失证据。当前结果必须为未就绪，而不是把已打包等同可发布。

## 下一阶段按顺序推进

1. **许可与来源**：确认 BS 权重明确分发许可、GAME 权重许可范围；保存正式模型许可文本与对应版本。完成 Qt、FFmpeg、PyTorch 和依赖通知/源码义务清单。不能将仓库许可标签或可下载视为最终审核完成。
2. **干净构建**：以独立环境安装锁定的 Python/PyTorch/应用依赖，固定 wheel 哈希、分离运行库及本地补丁。当前 Setup.ps1 是开发安装入口，尚不能用于完整正式构建。
3. **可靠性**：升级前备份及旧库迁移、异常退出恢复、缺模型/磁盘不足/损坏音频提示；检验分卷缺失与校验失败。GPU 小规模自检不代替完整模型兼容性测试。
4. **真实设备验收**：无 Python/开发环境的干净 Windows；实际低显存、低内存机器与不同显卡代际。已有同机 CPU/显存预算测试不代表物理低配测试，不发布未经验证的最低配置。
5. **正式交付**：完善中文安装/故障说明和版本记录，检查私有数据排除，决定代码签名，再构建并验收正式分卷包。发布前重新运行 release_check。

## 注意

此次环境自检改动尚未进入已经生成的安装器，按暂停打包决定留待统一构建。分卷漏文件异常场景尚未实测。没有创建 GitHub 仓库、上传附件、联系模型作者或代用户接受任何新许可。
